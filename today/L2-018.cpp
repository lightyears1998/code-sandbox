#include <bits/stdc++.h>
using namespace std;

int n, cur;
vector<int> a; int ah;
vector<int> b; int bh;
vector<int> q; int qh;
vector<int> r; int rh;

int main()
{
	cin >> n; cin >> ah;
	cin >> cur; a.push_back(cur);
	while (--n) {
		for (int i=ah-1; i>=0; --i) {
			if (i)
		}
	}
	
}
