#include <bits/stdc++.h>
using namespace std;

const int maxN = 1100;
const int  INF = 0x3F3F3F3F;

int mx;
int dp[maxN][maxN];
int c[maxN];
int cost[maxN];

int main()
{	
	int n, W, B, X; cin >> n >> W >> B >> X;
	memset(dp, 0x3F, sizeof(dp)); dp[0][0] = W;
	
	for (int i = 1; i <= n; ++i)
	{
		for (int j = 0; j < maxN; ++j)
		{
			
		}
	}
}

